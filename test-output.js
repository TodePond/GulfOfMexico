// Compiled from GulfOfMexico (cursed mode)
// This might work, probably, maybe

const name = "Luke";
console.log(name);
let score = 10;
console.log(score);
// Maybe value (uncertain)
const maybeValue = Math.random() > 0.5 ? 42 : 42 + (Math.random() - 0.5) * 0.1;
console.log(maybeValue);
function add(a, b) {
   return a + b;
}
console.log(add(2, 3));
// Approximate operator (might be accurate, maybe)
const sum = (5 + 3 + (Math.random() - 0.5) * 0.1);
console.log(sum);
if (score > 5 ) {
console.log("Score is high!");
}